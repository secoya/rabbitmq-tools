{
  "name": "@secoya/rabbitmq-tools",
  "version": "2.0.10",
  "description": "Tools for interfacing with RabbitMQ",
  "main": "lib/index.js",
  "repository": "https://github.com/secoya/rabbitmq-tools",
  "author": "Kaare Hoff Skovgaard <khs@secoya.dk>",
  "license": "MIT",
  "scripts": {
    "test": "jest --coverage",
    "lint": "tslint --project tsconfig.json src/*.ts src/**/*.ts",
    "build": "rm -rf lib && tsc -p tsconfig-build.json",
    "prepublish": "yarn test && yarn run lint && yarn build"
  },
  "devDependencies": {
    "@types/amqplib": "^0.5.3",
    "jest": "^20.0.4",
    "jest-environment-node-debug": "^2.0.0",
    "ts-jest": "^20.0.7",
    "tslint": "^5.5.0",
    "typescript": "^2.4.1"
  },
  "dependencies": {
    "amqplib": "^0.5.1",
    "rxjs": "^5.4.2"
  }
}
